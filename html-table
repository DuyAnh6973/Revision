
<style>
   table, tr, td{
       border: 1px solid grey;
       border-collapse: collapse;
       padding: 5px;
   }
</style>
<html>
   <body>
       <table>
           <tr>
               <td>Name</td>
               <td>Address</td>
               <td>Age</td>
           </tr>
           <tr>
               <td>Thanh</td>
               <td>Q7</td>
               <td>30</td>
           </tr>
           <tr>
               <td>Toan</td>
               <td>Q1</td>
               <td>20</td>
           </tr>
       </table>
   </body>
</html>











































MANPATH="$MANPATH:/usr/X11R6/man"
for month in {1..12..1} do
mkdir "$month" && touch "$month/$month's task"
done



git init 
git remote add origin git@github.com:username/reponame
git push origin
git add hello.txt && git commit -m "Add hello.txt"
cat "Hello World" >> hello.txt && git add hello.txt && git commit -m "add text"
git checkout -b branch1 && touch mybranch.txt && git add mybranch.txt && git commit --allow-empty-message
git checkout -b branch2 && touch mybranch.txt && git add mybranch.txt && git commit --allow-empty-message
